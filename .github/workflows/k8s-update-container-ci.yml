name: Build, Test & Publish Docker image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: ghcr.io/${{ github.repository }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract distro tag
        run: |
          DISTRO=$(awk '/^FROM / { split($2,a,":"); print a[2]; exit }' Dockerfile.dockerfile)
          echo "DISTRO=${DISTRO}" >> $GITHUB_ENV

      - name: Prepare Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & load AMD64 image (test)
        uses: docker/build-push-action@v4
        with:
          context: .
          file: Dockerfile.dockerfile
          platforms: linux/amd64
          push: false
          load: true
          cache-from: type=gha,scope=amd64
          cache-to: type=gha,mode=max,scope=amd64
          tags: ${{ env.IMAGE_NAME }}:test-amd64

      - name: Run container to validate
        run: |
          container_id=$(docker run -d ${{ env.IMAGE_NAME }}:test-amd64)
          trap 'docker rm -f "$container_id"' EXIT
          sleep 3
          docker logs "$container_id"
          docker exec "$container_id" pgrep sshd

      - name: Push AMD64 image to registry
        uses: docker/build-push-action@v4
        with:
          context: .
          file: Dockerfile.dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ env.DISTRO }}
            ${{ env.IMAGE_NAME }}:latest
